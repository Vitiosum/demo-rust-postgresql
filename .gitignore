# Rust
/target/
# Cargo.lock is intentionally committed for binary crates (required by Clever Cloud --locked build)

# Environment
.env

# Editor
.vscode/
.idea/
*.swp

# macOS
.DS_Store

# Clever Cloud
# .clever.json is tracked but committed empty: clever link / clever create rewrite it locally
