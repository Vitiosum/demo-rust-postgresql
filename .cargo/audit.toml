# Configuration de cargo-audit (audit des dépendances Rust)
[advisories]
ignore = [
    # RUSTSEC-2023-0071 — crate `rsa` (attaque Marvin, aucun correctif publié).
    # `rsa` n'est tiré que par `sqlx-mysql`, qui n'est pas compilé dans ce
    # projet (feature "postgres" uniquement) : `cargo tree -i rsa` → rien.
    "RUSTSEC-2023-0071",
]
